<UserControl x:Class="Kinectitude.Editor.Views.EntityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Kinectitude.Editor.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:AttributeViewModel}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Key}" />
                <TextBlock>: </TextBlock>
                <TextBlock Text="{Binding StringValue}" />
            </StackPanel>
        </DataTemplate>
        <!--DataTemplate DataType="{x:Type vm:ComponentViewModel}">
            <TextBlock Text="{Binding Type}" />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:EventViewModel}">
            <TextBlock Text="{Binding Type}" />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ActionViewModel}">
            <TextBlock Text="{Binding Type}" />
        </DataTemplate-->
    </UserControl.Resources>

    <ScrollViewer Background="Gray" HorizontalScrollBarVisibility="Visible">
        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" Margin="16" Background="LightGray">
            <!--Label Content="{Binding Name}" /-->
            <!--Separator />
            <TextBlock>Attributes</TextBlock>
            <ListBox ItemsSource="{Binding Attributes}" />
            <TextBlock>Components</TextBlock>
            <ListBox ItemsSource="{Binding Components}" />
            <Separator />
            <TextBlock>Events</TextBlock>
            <ListBox ItemsSource="{Binding Events}" />
            <Separator /-->
            
            <!--ItemsControl ItemsSource="{Binding Components}" Margin="8">
                <ItemsControl.Resources>
                    <DataTemplate DataType="{x:Type vm:ComponentViewModel}">
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" Padding="8" Background="DarkGray" />
                            <ItemsControl ItemsSource="{Binding Properties}">
                                <ItemsControl.Resources>
                                    <DataTemplate DataType="{x:Type vm:PropertyViewModel}">
                                        <TextBlock Text="{Binding Name}" />
                                    </DataTemplate>
                                </ItemsControl.Resources>
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Vertical" Background="White" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.Resources>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl-->

        </StackPanel>
    </ScrollViewer>

</UserControl>
